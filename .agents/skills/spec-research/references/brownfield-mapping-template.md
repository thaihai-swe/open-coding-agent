# Brownfield Map

## Subsystem Boundary
- Feature / subsystem:
- Entry point(s):
- Primary execution flow:
- Components / target files:
- Public seams (where behavior is observed):
- Data / API boundaries:

## Baseline Verification
- Existing tests / baseline command:
- Known broken tests (document, do not fix silently):
- Security-sensitive paths:
- Preserved behaviors (minimum 3 baseline invariants):

## Change Analysis
- High risk paths:
- Smallest safe change:
- Proof command / regression seam:
- Remaining uncertainty:
- Suggested handoff: `/spec-requirements` | `/spec-adr` | user
